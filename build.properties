passthru=true

dir.src=${project.basedir}/src
dir.build=${project.basedir}/build
dir.api-docs=${dir.build}/api
dir.code-coverage=${dir.build}/code-coverage
dir.code-browser=${dir.build}/code-browser
dir.pdepend=${dir.build}/pdepend
dir.xml-files=${dir.build}/xml
dir.bin=${project.basedir}/vendor/bin

dir.autophing=${project.basedir}/vendor/cac/autophing/lib

phplint.dir=${dir.src}

phpmd.log.file=${dir.xml-files}/phpmd.xml
phpmd.config=${phing.dir.baker}/phpmd.xml
phpmd.exclude=${project.basedir}/vendor

phpunit.config=${phing.dir.baker}/phpunit.build.xml

phpdoc.file=${dir.bin}/phpdoc
phpdoc.template=clean
phpdoc.source=${dir.src}

pdepend.xml.file=${dir.xml-files}/pdepend.xml
pdepend.source=${dir.src}
pdepend.chart.file=${dir.pdepend}/jdepend.svg
pdepend.pyramid.file=${dir.pdepend}/pyramid.svg

phpcs.checkstyle.file=${dir.xml-files}/phpcs.xml
phpcs.include.path=${dir.src}

phploc.file=${dir.bin}/phploc
phploc.xml.file=${dir.xml-files}/phploc.xml
phploc.source=${dir.src}

phpcpd.file=${dir.bin}/phpcpd
phpcpd.source=${dir.src}
phpcpd.xml.file=${dir.xml-files}/pmd-cpd.xml

phpcb.file=${dir.bin}/phpcb
phpcb.source=${dir.src}